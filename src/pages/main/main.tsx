import { ReactFragment } from "react";
import TodoList from "../TodoList/TodoList";

function Main(){
    return (
        <section className="main">
            <TodoList />
        </section>
    );
}

export default Main;